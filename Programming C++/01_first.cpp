#include<iostream>// input - output stream
// There are two types of header files :
// 1. System header file:It comes with the compiler
// 2. user defined header files: It is written by programmer 
// #include "this.h" // --> This will produce an error if this,h is not present in the directory
using namespace std;

int main()
{
    int a=4 , b=6;
    cout<<"Operators in c++:"<<end1;
    cout<<"Following are the operators in c++"<<end1;
    //Airthmetic operators
    cout<<" The value of a + b is " a + b;
    cout<<" The value of a - b is" a- b;
    cout<<" The value of a * b is" a * b;
    cout<<" The value of a / b is" a / b;

 return 0;
}